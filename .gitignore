.DS_Store
.env
build/
*.xcarchive
*.ipa
__pycache__/
# Pipeline runtime state — generated fresh by every build/test run. Nothing
# under .autobot/ is intended to be committed; if a future artefact needs to
# be tracked, add an explicit `!.autobot/<file>` negation below.
.autobot/
artifacts/
Samples/
